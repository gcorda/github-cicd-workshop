name: Docker CD Pipeline

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Print message
      uses: ./github/actions/prin_message
      with:
        message: I like to read my own message
      
        