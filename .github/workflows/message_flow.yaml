name: Docker CD Pipeline

on:
  push:
    branches:
      - master

jobs:
  print_message:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Print message
      uses: gcorda/github/actions/print_message
      with:
        message: I like to read my own message
      
        